% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataPrepDS.R
\name{semiOPBARTLocalHistogramDS}
\alias{semiOPBARTLocalHistogramDS}
\title{Report this site's local counts within a SHARED set of bin edges (the
same edges at every site, so summing is meaningful).}
\usage{
semiOPBARTLocalHistogramDS(data.name = "semiOPBART_train", bin_edges_Serialize)
}
\arguments{
\item{data.name}{the TRAIN object}

\item{bin_edges_Serialize}{Serialize-encoded named list, one numeric vector of
bin edges (length num_bins + 1) per column -- built once, client-side,
from the global range, and broadcast to every site so counts line up}
}
\description{
Report this site's local counts within a SHARED set of bin edges (the
same edges at every site, so summing is meaningful).
}
